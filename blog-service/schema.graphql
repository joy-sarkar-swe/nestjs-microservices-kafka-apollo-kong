# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""User reference stub (owned by user-service)"""
type User {
  """User UUID — federation linking key"""
  id: ID!
}

"""A blog post owned by blog-service"""
type Blog {
  """Unique blog post identifier (UUID v4)"""
  id: ID!

  """Post headline"""
  title: String!

  """Post body text"""
  content: String!

  """UUID of the author — references User.id in user-service"""
  authorId: ID!

  """Author resolved from user-service via Apollo Federation"""
  author: User
}

"""A single field-level validation error"""
type FieldError {
  """The input field that failed validation"""
  field: String!

  """Human-readable description of the failure"""
  message: String!
}

type Query {
  """Fetch all blog posts."""
  getBlogs: BlogsResponse!

  """Fetch one blog post by UUID."""
  getBlog(input: GetBlogArgs!): BlogResponse!
}

"""Returns BlogsSuccessResponse or ErrorResponse"""
union BlogsResponse = BlogsSuccessResponse | ErrorResponse

"""Success response wrapping a list of Blog entities"""
type BlogsSuccessResponse {
  success: Boolean!
  message: String!
  statusCode: Int!
  timestamp: DateTime!

  """The resolved Blog list"""
  data: [Blog!]!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

"""Returned when a blog-service operation fails"""
type ErrorResponse {
  """Always false for error responses"""
  success: Boolean!

  """Human-readable error summary"""
  message: String!

  """HTTP-equivalent status code"""
  statusCode: Int!

  """Server-side response timestamp"""
  timestamp: DateTime!

  """Field-level validation errors (present on status 400 only)"""
  errors: [FieldError!]
}

"""Returns BlogSuccessResponse or ErrorResponse"""
union BlogResponse = BlogSuccessResponse | ErrorResponse

"""Success response wrapping a single Blog entity"""
type BlogSuccessResponse {
  """Always true"""
  success: Boolean!

  """Human-readable success message"""
  message: String!

  """HTTP-equivalent status code"""
  statusCode: Int!

  """Server-side response timestamp"""
  timestamp: DateTime!

  """The resolved Blog entity"""
  data: Blog!
}

"""Arguments for fetching a blog post by ID"""
input GetBlogArgs {
  """UUID of the blog post to fetch"""
  id: ID!
}

type Mutation {
  """
  Create a blog post. Emits blog.created Kafka event. Subscribers receive push after Kafka consumer fires.
  """
  createBlog(input: CreateBlogInput!): BlogResponse!

  """Partially update a blog post. Emits blog.updated Kafka event."""
  updateBlog(input: UpdateBlogInput!): BlogResponse!

  """Delete a blog post. Emits blog.deleted Kafka event."""
  deleteBlog(input: DeleteBlogInput!): BaseApiResponse!
}

"""Input for creating a new blog post"""
input CreateBlogInput {
  """Post headline (2–200 characters)"""
  title: String!

  """Post body text (min 10 characters)"""
  content: String!

  """UUID of the author (must exist in user-service)"""
  authorId: ID!
}

"""Input for partially updating a blog post"""
input UpdateBlogInput {
  """UUID of the blog post to update"""
  id: ID!

  """New headline"""
  title: String

  """New body text"""
  content: String
}

"""Returns BaseResponse or ErrorResponse"""
union BaseApiResponse = BaseResponse | ErrorResponse

"""Generic success confirmation without a data payload"""
type BaseResponse {
  """Always true for success responses"""
  success: Boolean!

  """Human-readable success message"""
  message: String!

  """HTTP-equivalent status code"""
  statusCode: Int!

  """Server-side response timestamp"""
  timestamp: DateTime!
}

"""Input for deleting a blog post"""
input DeleteBlogInput {
  """UUID of the blog post to delete"""
  id: ID!
}

type Subscription {
  """Real-time push when a blog post is created."""
  blogCreated: Blog!

  """Real-time push when a blog post is updated."""
  blogUpdated: Blog!

  """
  Real-time push when a blog post is deleted. Returns the deleted blog id.
  """
  blogDeleted: String!
}